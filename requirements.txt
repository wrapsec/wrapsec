annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
async-timeout==5.0.1
asyncpg==0.31.0
certifi==2026.2.25
click==8.3.2
colorama==0.4.6
exceptiongroup==1.3.1
fastapi==0.135.3
greenlet==3.3.2
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
joblib==1.5.3
MarkupSafe==3.0.2
numpy==2.2.6
prometheus_client==0.24.1
pydantic==2.12.5
pydantic-settings==2.14.2
pydantic_core==2.41.5
python-dotenv==1.2.2
redis==7.4.0
pandas==2.2.3
scikit-learn==1.7.2
scipy==1.15.3
SQLAlchemy==2.0.49
alembic==1.14.1                # schema migrations - runs on API startup via db.session.run_migrations
starlette==1.3.1
threadpoolctl==3.6.0
tomli==2.4.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.44.0
aiosqlite==0.21.0

# ── JWT + Auth ────────────────────────────────────────────────────────────────
PyJWT==2.13.0                 # JWT encoding/decoding
passlib[argon2,bcrypt]==1.7.4 # password hashing — argon2 default, bcrypt kept for legacy verify
argon2_cffi==23.1.0           # Argon2id backend (B6 in v1.1.0; replaces bcrypt for new hashes)
bcrypt==4.0.1                 # passlib compat pin — bcrypt>=4.1 breaks passlib 1.7.4
python-multipart==0.0.32      # FastAPI form parsing — required for login endpoint
APScheduler==3.11.2           # background retention worker
email-validator==2.3.0

# ── Cryptography ──────────────────────────────────────────────────────────────
cryptography==49.0.0          # AES-256-GCM encryption for proxy provider API keys

# ── ML Detection -- Transformer (Tier 2) ─────────────────────────────────────
# Transformer dependencies are optional. Without them, transformer_detector degrades
# gracefully and TF-IDF (Tier 1) handles all detection.
# To install: pip install -r requirements-transformer.txt
# See docs/ml_detection_design.md for deployment impact (~1.5GB image increase).
